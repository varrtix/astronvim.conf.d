return {
  colorscheme = "xcodehc",

  plugins = {
    {
      "lunacookies/vim-colors-xcode",
      -- as = "vim-colors-xcode",
      -- config = function()
      -- require("vim-colors-xcode").setup {}
      -- end,
    }
  }
}
